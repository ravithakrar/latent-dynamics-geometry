# Everything needed to REDRAW the figures from the cached results in code/.
# Versions are the ones the dissertation figures were produced with; the scripts
# are not fussy about minor versions.
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
SciencePlots==2.2.2

# Only needed to REBUILD the caches from the raw recordings (see README, "Rebuilding
# the caches"). Not required for any figure. Install with:
#     pip install -r requirements-refit.txt
